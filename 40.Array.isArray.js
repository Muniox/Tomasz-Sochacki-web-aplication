const arr = [1, 2, 3];
const num = 5;
const str = 'jakiś tekst';

Array.isArray(arr); //true
Array.isArray(num); //false
Array.isArray(str); //false

//sprawdza czy dana zmienna jest tablicą i zwraca true jeśli jest, flase jesli nie jest